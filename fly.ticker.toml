app = "nightfall-ticker"
primary_region = "ord"

[build]
  dockerfile = "Dockerfile.ticker"

[env]
  NODE_ENV = "production"
  LOG_LEVEL = "info"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256
